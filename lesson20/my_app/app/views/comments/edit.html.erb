<h1><%= t('.title') %></h1>

<%= render 'form' %>

<%= link_to t('.back_to_post'), @post %> |
<td><%= link_to t('common.destroy'), @comment, method: :delete, data: { confirm: t('common.are_you_sure') } %></td>
