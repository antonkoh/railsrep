<%= form_for(@comment) do |f| %>
    <%= render 'common/errors', object: @comment %>

    <div class="field">
      <%= f.text_area :body %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
